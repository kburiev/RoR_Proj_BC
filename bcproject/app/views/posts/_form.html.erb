<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :biographie %>
    <%= form.text_area :biographie, id: :post_biographie %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :post_comment %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :post_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
